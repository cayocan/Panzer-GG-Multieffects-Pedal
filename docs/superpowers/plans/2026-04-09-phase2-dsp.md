# Phase 2: DSP Real — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Completar a cadeia DSP real do Panzer-GG adicionando 2x oversampling ao AmpProcessor, verificar todos os smoke tests e gerar build Release.

**Architecture:** Toda a cadeia DSP (Gate→Amp→Mod→Delay→Reverb→IRCab) já está implementada e conectada no PluginProcessor. O único gap em relação ao spec aprovado é a ausência de 2x oversampling no AmpProcessor para reduzir aliasing na etapa de waveshaping. Os smoke tests já existem em `tests/ProcessorSmokeTest.cpp`.

**Tech Stack:** JUCE 8, C++17, `juce::dsp::Oversampling<float>`, MSVC via VS2022

**CMake path:** `"C:/Program Files/Microsoft Visual Studio/2022/Community/Common7/IDE/CommonExtensions/Microsoft/CMake/CMake/bin/cmake.exe"`

---

## Mapa de Arquivos

| Arquivo | Ação | Responsabilidade |
|---------|------|-----------------|
| `src/dsp/AmpProcessor.h` | Modificar | Adicionar membro `juce::dsp::Oversampling<float>` |
| `src/dsp/AmpProcessor.cpp` | Modificar | Usar oversampling em `prepare()` e `process()` |
| `tests/ProcessorSmokeTest.cpp` | Verificar | Smoke tests já existentes — só rodar |

---

## Task 1: Adicionar 2x oversampling ao AmpProcessor

**Files:**
- Modify: `src/dsp/AmpProcessor.h`
- Modify: `src/dsp/AmpProcessor.cpp`

- [ ] **Step 1.1: Adicionar membro Oversampling ao header**

Modificar `src/dsp/AmpProcessor.h`. Substituir o bloco `private:` existente:

```cpp
private:
    std::atomic<float>* ampType;
    std::atomic<float>* gain;
    std::atomic<float>* treble;
    std::atomic<float>* middle;
    std::atomic<float>* bass;
    std::atomic<float>* ampVol;

    double sampleRate = 44100.0;

    static constexpr int kMaxChannels = 2;

    using IIRCoeff  = juce::dsp::IIR::Coefficients<float>;
    using IIRFilter = juce::dsp::IIR::Filter<float>;

    IIRFilter bassFilters  [kMaxChannels];
    IIRFilter midFilters   [kMaxChannels];
    IIRFilter trebleFilters[kMaxChannels];

    float cachedBass   = -9999.f;
    float cachedMiddle = -9999.f;
    float cachedTreble = -9999.f;

    void  updateEQIfChanged (float t, float m, float b);
    static float waveshape (float x, int type);
};
```

Substituir por:

```cpp
private:
    std::atomic<float>* ampType;
    std::atomic<float>* gain;
    std::atomic<float>* treble;
    std::atomic<float>* middle;
    std::atomic<float>* bass;
    std::atomic<float>* ampVol;

    double sampleRate = 44100.0;

    static constexpr int kMaxChannels = 2;

    // 2x oversampling — applied only around the waveshaper to reduce aliasing
    juce::dsp::Oversampling<float> oversampling {
        kMaxChannels,
        1,                                                      // factor = 2^1 = 2x
        juce::dsp::Oversampling<float>::filterHalfBandPolyphaseIIR,
        true };                                                 // maximally flat

    using IIRCoeff  = juce::dsp::IIR::Coefficients<float>;
    using IIRFilter = juce::dsp::IIR::Filter<float>;

    IIRFilter bassFilters  [kMaxChannels];
    IIRFilter midFilters   [kMaxChannels];
    IIRFilter trebleFilters[kMaxChannels];

    float cachedBass   = -9999.f;
    float cachedMiddle = -9999.f;
    float cachedTreble = -9999.f;

    void  updateEQIfChanged (float t, float m, float b);
    static float waveshape (float x, int type);
};
```

- [ ] **Step 1.2: Inicializar oversampling em prepare()**

Em `src/dsp/AmpProcessor.cpp`, substituir `prepare()`:

```cpp
void AmpProcessor::prepare (double sr, int blockSize)
{
    sampleRate = sr * 2.0;   // store oversampled rate — EQ coefficients use this
    cachedBass = cachedMiddle = cachedTreble = -9999.f;

    oversampling.initProcessing ((size_t) blockSize);

    updateEQIfChanged (50.f, 50.f, 50.f); // init flat EQ at oversampled rate

    reset();
}
```

- [ ] **Step 1.3: Adicionar reset do oversampling em reset()**

Em `src/dsp/AmpProcessor.cpp`, substituir `reset()`:

```cpp
void AmpProcessor::reset()
{
    oversampling.reset();
    for (int ch = 0; ch < kMaxChannels; ++ch)
    {
        bassFilters  [ch].reset();
        midFilters   [ch].reset();
        trebleFilters[ch].reset();
    }
}
```

- [ ] **Step 1.4: Usar oversampling em process()**

Em `src/dsp/AmpProcessor.cpp`, substituir `process()`:

```cpp
void AmpProcessor::process (juce::AudioBuffer<float>& buffer)
{
    const int   type    = static_cast<int> (ampType->load());
    const float gainVal = gain->load();
    const float t       = treble->load();
    const float m       = middle->load();
    const float b       = bass->load();
    const float vol     = ampVol->load();

    updateEQIfChanged (t, m, b);

    const float preGain    = std::pow (10.f, juce::jmap (gainVal, 0.f, 100.f, -1.f, 1.5f));
    const float outputGain = juce::jmap (vol, 0.f, 100.f, 0.f, 1.5f);

    const int numCh = juce::jmin (buffer.getNumChannels(), kMaxChannels);

    // ── Up-sample ────────────────────────────────────────────────────────
    juce::dsp::AudioBlock<float> inputBlock (buffer);
    auto oversampledBlock = oversampling.processSamplesUp (inputBlock);

    const int numOsSamples = (int) oversampledBlock.getNumSamples();

    // ── Waveshape + EQ at 2× sample rate ────────────────────────────────
    for (int ch = 0; ch < numCh; ++ch)
    {
        auto* data = oversampledBlock.getChannelPointer (ch);

        for (int s = 0; s < numOsSamples; ++s)
        {
            float x = waveshape (data[s] * preGain, type);
            x = bassFilters  [ch].processSample (x);
            x = midFilters   [ch].processSample (x);
            x = trebleFilters[ch].processSample (x);
            data[s] = x * outputGain;
        }
    }

    // ── Down-sample back to native rate ──────────────────────────────────
    oversampling.processSamplesDown (inputBlock);
}
```

- [ ] **Step 1.5: Build Debug para verificar que compila**

```bash
CMAKE="C:/Program Files/Microsoft Visual Studio/2022/Community/Common7/IDE/CommonExtensions/Microsoft/CMake/CMake/bin/cmake.exe"
"$CMAKE" --build build --target PanzerGG_VST3 --config Debug 2>&1 | grep -E "error C|Build succeeded|FAILED" | head -20
```

Esperado: sem linhas com `error C`. O build termina com sucesso.

Se falhar com `oversampling.processSamplesUp` não encontrado: verificar que `juce::dsp_` está linkado no CMakeLists.txt (já está via `juce::juce_dsp`).

- [ ] **Step 1.6: Commit**

```bash
git add src/dsp/AmpProcessor.h src/dsp/AmpProcessor.cpp
git commit -m "feat: add 2x oversampling to AmpProcessor waveshaper"
```

---

## Task 2: Rodar os smoke tests

**Files:**
- Test: `tests/ProcessorSmokeTest.cpp` (já existe, só compilar e rodar)

- [ ] **Step 2.1: Build do target de testes**

```bash
CMAKE="C:/Program Files/Microsoft Visual Studio/2022/Community/Common7/IDE/CommonExtensions/Microsoft/CMake/CMake/bin/cmake.exe"
"$CMAKE" --build build --target PanzerGGTests --config Debug 2>&1 | grep -E "error C|->|FAILED" | head -20
```

Esperado: linha com `-> PanzerGGTests.exe` sem erros.

- [ ] **Step 2.2: Rodar os testes**

```bash
"build/PanzerGGTests_artefacts/Debug/PanzerGGTests.exe"
```

Esperado: saída terminando com `All tests passed` ou similar. Nenhum `FAILED`.

Se algum teste falhar:
- `GateProcessor silences signal` falhar → verificar coeficientes de attack/release em `GateProcessor.cpp`
- `AmpProcessor NaN/Inf` → verificar que `waveshape()` não produz infinito para entrada grande com preGain alto
- `Reverb Cloud tail` → verificar que feedback coefficient `g` está abaixo de 1.0

- [ ] **Step 2.3: Commit (somente se houver correções)**

Se nenhuma correção foi necessária, pular este commit.

```bash
git add tests/ src/dsp/
git commit -m "fix: correct DSP module behavior per smoke test failures"
```

---

## Task 3: Build Release

**Files:** nenhum arquivo novo

- [ ] **Step 3.1: Build Release VST3 + Standalone**

```bash
CMAKE="C:/Program Files/Microsoft Visual Studio/2022/Community/Common7/IDE/CommonExtensions/Microsoft/CMake/CMake/bin/cmake.exe"
"$CMAKE" --build build --target PanzerGG_VST3 --config Release 2>&1 | grep -E "error C|->|FAILED" | tail -5
"$CMAKE" --build build --target PanzerGG_Standalone --config Release 2>&1 | grep -E "error C|->|FAILED" | tail -5
```

Esperado:
```
PanzerGG_VST3.vcxproj -> ...Panzer-GG.vst3
PanzerGG_Standalone.vcxproj -> ...Panzer-GG.exe
```

- [ ] **Step 3.2: Verificar o Standalone abre sem crash**

Abrir manualmente:
```
build\PanzerGG_artefacts\Release\Standalone\Panzer-GG.exe
```

Esperado: janela 860×280px abre sem crash, áudio flui com processamento ativo.

- [ ] **Step 3.3: Commit final da fase**

```bash
git add .
git commit -m "feat: Phase 2 DSP complete — oversampling, tests passing, Release build"
```
